
import React from 'react';

// This component is deprecated and no longer used in the application.
// Keeping the file to prevent build errors if it's still referenced somewhere,
// but it renders nothing.
const CoinAlertsPage: React.FC<any> = () => {
  return null;
};

export default CoinAlertsPage;
